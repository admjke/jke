# Default values for jke.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameSpace: hcl-app

replicaCount: 1

service:
  type: ClusterIP

image:
  repository: myclusterhcl3.icp:8500
  name: jke-mq
  tag: latest
  pullPolicy: Always