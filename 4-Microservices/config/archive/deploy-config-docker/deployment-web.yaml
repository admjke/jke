apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: jke-web
  namespace: hclapp
  labels:
    app: jke-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jke-web
  template:
    metadata:
      labels:
        app: jke-web
    spec:
      containers:
        - image: gandigit/jke-web:latest
          name: jke-web
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http-server
      imagePullSecrets:
        - name: jke-docker-registry-secret