apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Values.projectNameMQ }}
  # namespace: {{ .Values.nameSpace }}
  labels:
    app: {{ .Values.projectNameMQ }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.projectNameMQ }}
  template:
    metadata:
      labels:
        app: {{ .Values.projectNameMQ }}
    spec:
      containers:
        - image: {{ .Values.dockerImageNameMQ }}
          name: {{ .Values.projectNameMQ }}
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http-server
      imagePullSecrets:
        - name: {{ .Values.dockerImagePullSecretName }}