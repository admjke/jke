apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Values.projectNameBankStreet }}
  # namespace: {{ .Values.nameSpace }}
  labels:
    app: {{ .Values.projectNameBankStreet }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.projectNameBankStreet }}
  template:
    metadata:
      labels:
        app: {{ .Values.projectNameBankStreet }}
    spec:
      containers:
        - image: {{ .Values.dockerImageNameBankStreet }}
          name: {{ .Values.projectNameBankStreet }}
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http-server
      imagePullSecrets:
        - name: {{ .Values.dockerImagePullSecretName }}